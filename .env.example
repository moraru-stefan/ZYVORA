# TMDB API key (get one at https://www.themoviedb.org/settings/api)
# Note: this is a frontend-only project, so any VITE_ variable is exposed
# in the client bundle at build time. It is not a server-side secret.
VITE_TMDB_API_KEY=your_tmdb_api_key_here
